// const Arr =['children', 'school']
// const replace ="ch"
// Regex

// 3. arr.forEach(item =>{
//     let findIndex = null

//     do {
//         const nextFindIndex = item.indexOf('ch')
//         findIndex= nextFindIndex
//     }while (findIndex !== -1)
// })

// 2 replaceAll('ch', '<b><u>ch</u></b>')
// 1 const transformArr = arr.map(item => item.replace(/ch/, '<b><u>ch</u></b>'))

// transformArr.map(item => <Test data={item}/>)

export const Test = ({ data }: any) => {
  //   return <div dangerouslySetInnerHTML={}></div>
  return <></>
}
